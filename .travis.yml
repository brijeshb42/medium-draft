language: node_js
before_install:
  - yarn && ./node_modules/.bin/lerna bootstrap
script:
  - yarn run test
node_js:
  - "8"
  - "7"
after_success:
  - bash <(curl -s https://codecov.io/bash)
cache:
  yarn: true
  directories:
    - node_modules
